# Database
DATABASE_URL=sqlite+aiosqlite:///./data/open_codex.db

# Server
HOST=127.0.0.1
PORT=8000
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Docker
DOCKER_CONTAINER_POOL_SIZE=5

# LLM (Optional defaults, can be overridden per project)
DEFAULT_LLM_PROVIDER=openai
DEFAULT_LLM_MODEL=gpt-4o-mini

# API Key Encryption
# Master encryption key for securing API keys in database
# Generate a new key with: poetry run python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
MASTER_ENCRYPTION_KEY=rQG1DMz9O121aR9jZ7GrFUFAIVlnMNz-eaxWBpvYwnQ=

# Legacy: Environment-based API keys (fallback if not set in database UI)
# You can now manage API keys through the Settings page in the web UI
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# AZURE_API_KEY=...
